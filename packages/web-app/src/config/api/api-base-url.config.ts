export const BASE_URL: string = 'http://localhost'; // TODO: Make it fallback and use dotenv variable at first
export const PORT: string | number = 4000; // TODO: Make it fallback and use dotenv variable at first

export const API_BASE_URL: string = `${BASE_URL}:${PORT}`;
